'use strict';
define([
    //Dependencies (by requirejs) ex: paths service to inject
    'js/common/controller',
    'js/directives/pzDocumentEditor'
], function(app){
    app.controller('HomePage',['$scope', '$http','$sce',function($scope, $http, $sce) {
        
    }]);
})